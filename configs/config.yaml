paths:
  data_csv: data/bank-full.csv
  processed_dir: data/processed
  results_dir: results
  checkpoint_dir: checkpoints

training:
  seed: 42
  batch_size: 256
  learning_rate: 0.001
  epochs: 30
  patience: 5

model:
  embedding_dim: 32
  nhead: 4
  num_layers: 2
  feedforward_dim: 128
  dropout: 0.1

experiment:
  include_duration: true
